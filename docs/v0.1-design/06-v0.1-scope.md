# V0.1 design — Q6: V0.1 scope

> **Status: LEADING (V0.1).** Brainstorm output — the scope decisions the plan turns into steps.
> Not gospel. Entry: [../START-HERE.md](../START-HERE.md). Builds on
> [SYNTHESIS](../v0.1-cases/SYNTHESIS.md), [00 reuse-inventory](00-v0-reuse-inventory.md), and Q2–Q5/Q7.
> _2026-06-17._

## The framing
**Build V0.1 now** = **the real product experience for the common path, end-to-end**, narrow in
lever-coverage. Complete enough to be real; narrow enough to ship. The cut below optimizes the
**triage center of gravity** first (don't-pay-yet / looks-fine), then a small set of dispute levers
behind it. _The **June 27 hackathon is a downstream checkpoint** we'll cherry-pick from, not the
organizing target. With current coding models the build is fast; **testing is the pacing constraint**,
so scope is bounded by what we can test well (see [07-eval-and-safety](07-eval-and-safety.md))._

## IN — the spine, end-to-end
1. **Chat-first (mobile-first) UI** with a **minimal inline component catalog** — **80/20: build only
   the most-expected cards first** (the **verdict card** + **amounts panel** are the hero 20%), then
   **doc chip + viewer** and **risk-tiered confirm buttons**; **intake = conversational** (stub the
   mini-form as text), **activity log = a light text list** to start. Owned, accessible, text-fallback
   each. Empty state with suggested starts (no blank box). Grow the catalog from there. (Q4)
2. **Intake & triage for the common path** (Q2): any-shape input (upload / forward / paste / ask) →
   **document-type detection** (statement vs itemized vs EOB) → **insurance situation** → file into
   **case → bill → documents + amounts** → one of the **4 common verdicts** (don't-pay-yet / looks-fine /
   something's-off / need-more). Conservative on the false-"pay it."
3. **The agent loop** (Q3): the durable orchestrator calling tools, with the **bright line enforced**
   (numbers/verdicts only from deterministic sources; validator on output).
4. **Tools wired:** document **parse** (reuse, agent-invoked) · **KB lookup** (new, seeded for the demo
   levers) · the **engine** (reuse as-is, on its home turf) · **artifact drafting** (reuse the letters
   layer) · the **case/bill/docs/amounts** store (reuse schema, adapted).
5. **A small set of levers fully working** (not the whole arsenal) — chosen to be high-frequency,
   demoable, and to span the situation→lever map:
   - **"Don't pay yet — it's a statement"** (doc-type driven; the #1 common verdict).
   - **"Looks correct — OK to pay"** (EOB↔bill reconciliation via amounts + savings-diff; the #2).
   - **Engine coding flag** on its home turf — **duplicate / unbundled (NCCI)** and/or
     **preventive-vs-diagnostic** — → "something's off."
   - **One assertion-of-right lever end-to-end with a drafted artifact:** recommend **ACA §2713
     preventive-$0** *or* **charity care / 501(r)** (pick one in the plan), draft the letter (bright-line),
     confirm-to-send.
6. **KB seeded from the cases** for exactly the chosen levers (full rule schema; populate only what the
   demo needs — versioning/citations included). (Q5)
7. **Eval harness extended** (Q7): doc-type + verdict classification on a handful of the case fixtures,
   plus the inherited engine golden eval and bright-line groundedness check.

## DEFERRED — fast-follow, explicitly not in V0.1
- The **full durable multi-week campaign engine** (escalation ladders, auto-cadence). *Stub* the
  deadline scheduler; don't build the multi-week runner.
- **Press / public "wall"** + journalist outreach.
- **Integrations** (insurer portal, FHIR) — accelerants, not dependencies. _(Exception to watch:
  **MCP** — exposing billcheck inside ChatGPT/Claude is a **distribution** play, not an integration
  dependency; it answers "why not just use ChatGPT." Still fast-follow, but strategically prioritized — see Q4.)_
- **Voice = speech-to-text input only** (dictate instead of type), not a 2-way voice agent. Cheap/free
  out-of-the-box (the mobile keyboard's own dictation already works in any text field; Web Speech API /
  Whisper for in-app). Defer until text works on its own.
- The **long tail of levers** and the **full 50-state KB**.
- Rich timeline/deadline-tracking UI polish.

## Build fresh (pure greenfield — V0 is reference only)
Per the [pure-greenfield decision](../v0.1-design-notes.md), **build every part anew**; do not adapt V0
code. Keep only the **good patterns** V0 proved (re-authored, not migrated) and the **acceptance bar**:
- **Re-author (don't reuse):** the deterministic **audit engine** + verdict router; the **bright-line
  artifact gate** (a fresh `validateLetter`-style validator); the **agent-loop LLM client** with guards
  baked in (PHASE-style gate, spend kill-switch, PHI-safe logging, ledger); the **schema/RLS**; parsing.
- **Carry over as the acceptance bar (tests, not code):** the **31 cases** + the engine golden fixtures'
  *properties* (anti-circular, injection-resilience, reproducibility), re-expressed for the new design.
- **Don't rebuild what V0 wrongly centered:** the linear funnel / click-through UI / verdict-as-endpoint —
  those were the old product; the new shape is the chat-first advisor (Q2–Q4).

## Stack
**Mobile-first responsive web** (not native). Next.js 16 / React 19 · Supabase · **Vercel AI SDK** (typed
tool-parts cards + `needsApproval` HITL; not the paused RSC `streamUI`) · **shadcn/ui** owned components ·
a **fresh single agent-loop LLM client** with guards baked in. **Model:** greenfield removes the "stay
pinned to `claude-sonnet-4-6` for calibration" constraint (we're rebuilding the engine/parse, so there's
nothing to regress) → **default to the latest, most-capable Claude model**, model id behind config; verify
the current id/pricing against live Anthropic docs before wiring.

## The demo narrative (proof the architecture works end-to-end)
1. User uploads a hospital **statement** → agent: **"Don't pay yet — this is a summary, not the itemized
   bill; watch for the EOB."** (verdict card + doc chip). 
2. User uploads the **itemized bill + EOB** → agent reconciles (amounts panel). Two branches, both real:
   - **Clean:** **"This checks out — OK to pay,"** with the reconciliation shown.
   - **Flagged:** engine finds a **duplicate/unbundled** line (or preventive miscoding) → **"Something's
     off,"** ranked options, offers to **draft an appeal/dispute** citing the KB rule.
3. Agent **drafts the letter** (numbers injected from findings, `validateLetter` passes) → **confirm to
   send.** Activity log shows the paper trail.

This exercises: any-shape intake, doc-type detection, insurance-situation routing, the engine as a tool,
the KB, bright-line artifact drafting, HITL confirm, and the case/bill/amounts/activity model.

## Cut line (if time gets short)
Absolute minimum that's still a coherent product: the **common triage path** (don't-pay-yet + looks-fine)
with doc-type detection, the amounts panel, **one** engine check, and **one** drafted letter behind a
confirm. Everything in IN-#5 beyond that is the first thing to trim.

## Open questions — now resolved in [PLAN.md](PLAN.md)
- Assertion-of-right lever → **ACA §2713 preventive-$0** (composes with the engine coding flag).
- Model → **stay on pinned `claude-sonnet-4-6`** for the structured tools; spike a newer model only for
  the conversation, behind `BILLCHECK_MODEL`.
- UI → **AI SDK typed-tool-parts card pattern** (not the paused RSC `streamUI`); run the loop on the
  shared client (review P0-1).
- Schema → **keep `cases.state` as-is**; narrate the living thread in `coverage_profile`/`case_events`
  (review P0-2) — net migration ≈ zero.
