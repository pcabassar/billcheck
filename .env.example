# Supabase (project: billcheck / etakonvmsfkyjnwksydi)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=   # server-only: purge route, cron reconcile, WDK steps (see AGENTS.md key map)

# Anthropic — single entry point: packages/shared/src/llm/client.ts
ANTHROPIC_API_KEY=
BILLCHECK_MODEL=claude-sonnet-4-6
BILLCHECK_PHASE=A            # PHASE=B only when BAA stack is on (see AGENTS.md)

# Stripe (test mode only until LLC trigger)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email (Resend) — auth mail + S5 reminder
RESEND_API_KEY=
