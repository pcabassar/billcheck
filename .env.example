# Supabase — PUBLIC values (safe to commit; the publishable key ships in the browser bundle)
NEXT_PUBLIC_SUPABASE_URL=https://etakonvmsfkyjnwksydi.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_FUUJfrioK8MlyqLCCyXYLg_2cCyAYZH

# SECRETS — set these in .env.local AND the Vercel project; never commit real values.
# SUPABASE_SECRET_KEY: Supabase dashboard -> Project Settings -> API Keys -> "secret" key (sb_secret_...)
SUPABASE_SECRET_KEY=
# DATABASE_URL: Supabase -> Connect -> Transaction pooler (Supavisor, port 6543), with your DB password
DATABASE_URL=
# Existing (from the prototype):
AI_GATEWAY_API_KEY=
BLOB_READ_WRITE_TOKEN=
# Reminder email (U6):
RESEND_API_KEY=
